ASM := nasm
ASM_OPTION := -f elf
ASM_OUTPUT_OPTION := -o

CC := gcc
CC_OPTION := -c -Wall
CC_OUTPUT_OPTION := -o

all: kernel.o start.o

%.o: %.asm
	$(ASM) $(ASM_OPTION) $< $(ASM_OUTPUT_OPTION) $@

%.o: %.c
	$(CC) $(CC_OPTION) $< $(CC_OUTPUT_OPTION) $@